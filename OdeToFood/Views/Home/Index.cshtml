@*@model IEnumerable<OdeToFood.Models.RestarauntListViewModel>*@
@model IPagedList<RestarauntListViewModel>
@{
    ViewBag.Title = "Home Page";
}

@*<form method="get">
    <input type="search" name="searchTerm" />
    <input type="submit" value="search by name" />

</form>*@

@*@using (Ajax.BeginForm(new AjaxOptions { HttpMethod = "get", InsertionMode = InsertionMode.Replace, UpdateTargetId = "restarauntList" }))
{
    <input type="search" name="searchTerm" />
    <input type="submit" value="search by name" />
}*@

<form method="get" action="@Url.Action("Index")" data-otf-ajax="true" data-otf-target="#restarauntList">
    <input type="search" name="searchTerm" data-otf-autocomplete="@Url.Action("Autocomplete")" />
    <input type="submit" value="search by name" />

</form>

@*@Html.Action("SayHello")*@
@{
    var amount = 10.0m;
    var someDate = new DateTime(2002, 7, 9);
}

<div>
    @OdeToFood.Views.Home.Resource.Greeting
</div>
<div>
    @amount.ToString("c")
</div>
<dv>
    @someDate.ToShortDateString()
</dv>

@Html.Partial("_Restaraunts",Model)